---
label: Blog post
hide_body: false
fields:
- type: text
  name: title
  label: title
- type: datetime
  name: date
  label: date
- name: gallery
  type: image_gallery
  config: {}
  label: gallery
- name: pick_color
  type: color
  config:
    required: false
    color_format: Hex
  label: pick color
- name: repeater
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: false
    label: name
  - name: email
    type: text
    config:
      required: false
    label: email
  - name: avatar
    type: file
    config:
      maxSize: 250
    label: avatar
  config:
    min: 
    max: 
    labelField: 
  label: repeater
- name: sortable
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: sortable
pages:
- content/posts/my-second-post.md
